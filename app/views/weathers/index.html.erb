<h1>Weather Songs</h1>

<% if @weathers.any? %>
  <% @weathers.each do |weather| %>
    <p><%= link_to "#{weather.response['temperature']}°F in #{weather.address} #{weather.created_at.strftime("at %I:%M%p on %m/%d/%Y")}", weather %></p>
  <% end %>
<% end %>


<%= form_for @weather do |f| %>
  <%= f.text_field :address %>
  <%= f.submit %>
<% end %>
